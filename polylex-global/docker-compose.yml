version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: polylex_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: polylex
      POSTGRES_USER: polylex
      POSTGRES_PASSWORD: polylex
    ports:
      - '5434:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - polylex-net
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U polylex -d polylex']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: polylex_redis
    restart: unless-stopped
    ports:
      - '6388:6379'
    volumes:
      - redisdata:/data
    networks:
      - polylex-net
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  whisper-stt:
    build:
      context: .
      dockerfile: apps/whisper-stt/Dockerfile
    container_name: polylex_whisper_stt
    restart: unless-stopped
    ports:
      - '9000:9000'
    networks:
      - polylex-net
    healthcheck:
      test: ['CMD', 'python', '-c', "import urllib.request; urllib.request.urlopen('http://127.0.0.1:9000/health')"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  redisdata:

networks:
  polylex-net:
    driver: bridge
